## Host Adapter: Claude Code

- Run inside one `research-paper-review` invocation for one validated document.
- Use the plugin's PDF text-index and bounded page or section retrieval operations; avoid loading the full text at once.
- Resolve only assigned claim windows and return evidence locations as artifact-relative refs.
