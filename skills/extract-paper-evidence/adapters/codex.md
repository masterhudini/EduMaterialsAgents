## Host Adapter: Codex

- Run inside an isolated paper-review node and use configured MCP text-index and bounded-read operations.
- Do not place the full extracted document in the node prompt or global context.
- If text indexing or isolated node execution is unavailable, return `external_dependency_blocked`.
