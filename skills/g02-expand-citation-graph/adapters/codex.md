## Host Adapter: Codex

- Use MCP `research_citation_expand`; pass the complete prepared canonical or recent input as
  `discovery_input` and structured
  seed, provider, relation, cursor and limit values.
- Pass seed IDs, allowed relations and limits as structured input; preserve returned edge provenance.
- Do not emulate citation edges when the MCP operation is unavailable.
