## Host Adapter: Codex

- Run inside an isolated node and resolve authorized content refs through the Research Graph tool surface.
- Keep role reasoning separate from MCP metadata and access fields.
- In A04 keep role, maturity, update class and quality status in separate annotation fields.
- Return `external_dependency_blocked` when required artifact resolution is unavailable.
