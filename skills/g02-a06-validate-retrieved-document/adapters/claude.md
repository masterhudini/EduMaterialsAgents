## Host Adapter: Claude Code

- Invoke `research_document_validate` on the temporary artifact descriptor.
- Promote a file only after signature, identity and checksum checks pass.
- Return structured rejection and keep invalid content outside the accepted corpus.
