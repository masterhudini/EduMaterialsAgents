## Host Adapter: Codex

- Use the configured MCP deduplication operation and preserve its merge rules and ambiguity groups.
- Do not perform filesystem-wide or web-based matching outside the supplied records.
- Report the missing MCP operation as `external_dependency_blocked`.
