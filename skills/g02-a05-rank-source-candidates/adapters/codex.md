## Host Adapter: Codex

- Apply ranking inside the isolated candidate-index node and use the configured scoring operation.
- Preserve component scores and deterministic tie ordering from the tool result.
- If the scoring operation is unavailable, return an external dependency issue rather than hidden weights.
