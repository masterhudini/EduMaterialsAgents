## Host Adapter: Claude Code

- Apply ranking inside `research-candidate-source-index` using the supplied selection profile.
- Use deterministic scoring support when exposed by the plugin and keep semantic rationale in the agent.
- Return recommendations only; never invoke the user gate directly.
