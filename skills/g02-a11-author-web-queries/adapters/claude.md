## Host Adapter: Claude Code

- Author the query strings only; do not run the search here (that is `g02-a11-find-market-cases`).
- Anchor every query in the topic `core_terms`; expand only with `allowed_expansion_areas`.
- Cover applied-case, failure-case and current-event angles; keep the set small (≈4–8 per topic).
- Write queries in the task `output_language` plus an English variant where it helps.
- These are plain user-facing search strings — no operators, endpoints or credentials.
