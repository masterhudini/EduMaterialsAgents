## Host Adapter: Codex

- Author the bounded query strings only; the host web search runs in `g02-a11-find-market-cases`.
- Keep queries anchored in the topic `core_terms` (plus `allowed_expansion_areas` only).
- Span applied-case, failure-case and current-event angles; keep the set small per topic.
- Do not call any provider or fetch any page in this skill.
