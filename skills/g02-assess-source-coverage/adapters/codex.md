## Host Adapter: Codex

- Use the configured MCP coverage operation for deterministic counts and requirement matching.
- Perform semantic interpretation inside the isolated node and preserve accepted human exceptions.
- Return `external_dependency_blocked` when required refs or the operation cannot be resolved.
