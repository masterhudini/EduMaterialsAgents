## Host Adapter: Claude Code

- Run coverage assessment inside the calling producer node with only supplied requirements and refs.
- Use deterministic counting or matrix validation exposed by the plugin; keep semantic gap reasoning in the node.
- Return CoverageMatrix to the caller and never open a human gate directly.
