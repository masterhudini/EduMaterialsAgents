## Host Adapter: Codex

- Run inside an isolated planner node supplied by the Codex node-agent adapter.
- Use the approved scoped bundle and resolve only explicitly authorized artifact refs.
- If isolated node execution is unavailable, report `external_dependency_blocked` to the orchestrator.
