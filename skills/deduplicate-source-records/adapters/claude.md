## Host Adapter: Claude Code

- Use the plugin's deterministic deduplication operation for exact IDs and conservative match groups.
- Review ambiguous groups semantically only within the calling index node.
- Preserve the complete merge log and stable source IDs.
