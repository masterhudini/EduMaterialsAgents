## Host Adapter: Claude Code

- Use `research_document_retrieve` with a verified OA resolution and confirmed source action.
- Keep temporary files inside the artifact workspace and pass only descriptors to subsequent validation.
- Never use unrestricted shell download commands as a fallback.
