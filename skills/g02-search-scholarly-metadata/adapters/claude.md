## Host Adapter: Claude Code

- Use the secret-free capabilities prepared by the owning node and call
  `research_metadata_search` per approved route with exactly one of `domain_input` or
  `canonical_input` or `recent_input`.
- Pass only structured QueryPlan data and consume normalized records plus artifact provenance.
- Never parse ad hoc web results or put credentials in the agent context.
- Preserve unavailable and zero-result operations in the owning operation log.
