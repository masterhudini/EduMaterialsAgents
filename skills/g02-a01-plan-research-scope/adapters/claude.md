## Host Adapter: Claude Code

- Run inside `g02-a01-planner`; use only the scoped input supplied by the orchestrator.
- Resolve approved artifact refs lazily with the host artifact surface and avoid literature tools.
- Return ResearchPlan through the node envelope without addressing the user.
