## Host Adapter: Claude Code

- Generate annotations inside the candidate-index node from authorized metadata, abstract or contents refs.
- Use the host artifact reader for referenced text and respect `output_language`.
- Return annotation fields to the index; do not converse with the user.
