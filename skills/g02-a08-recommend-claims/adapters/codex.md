## Host Adapter: Codex

- Bind A09's `scholarly_synthesis` and A11's `web_cases` only; do not search the web.
- Produce per-topic `recommended_claims` with matching `support_basis` and refs.
- Recommend additions at the research/topic level; no slide text, no placement, no critique.
- Finalize through MCP `research_a08_finalize`; omit `output` for the deterministic fallback.
