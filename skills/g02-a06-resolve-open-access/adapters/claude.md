## Host Adapter: Claude Code

- Call `research_oa_resolve` only for confirmed DOWNLOAD source IDs in `retrieval_input@1`.
- Pass stable source identifiers and preserve every checked route, version and license field.
- If no resolver tool is configured, return an external dependency issue without browsing around access controls.
