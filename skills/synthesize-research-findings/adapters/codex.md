## Host Adapter: Codex

- Run inside an isolated synthesizer node and hydrate refs through the configured Research Graph MCP surface.
- Keep full PDFs and verbose reviews outside the prompt and downstream handoff.
- If isolated node execution or artifact hydration is unavailable, return `external_dependency_blocked`.
